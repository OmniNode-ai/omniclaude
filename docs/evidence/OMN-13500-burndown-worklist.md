# OMN-13500 — omniclaude no-faked-boundary burn-down worklist

Generated: 2026-07-03
Detector: `uv run python -m omnibase_core.validation.no_faked_boundary.runtime_no_faked_boundary --report-only .`
(omnibase-core 0.46.4, wired report-only via omniclaude PR #1846, merged 2026-07-03T16:34:02Z)

Total findings: **61** (55 `mock_assigned_to_boundary` + 6 `patch_httpx_egress`), matching the
count cited in `.pre-commit-config.yaml` for the OMN-13501/13502 burn-down phase.

## Scope note (discrepancy flagged, not resolved here)

The Linear ticket body for OMN-13500 describes a narrower/different scope: "39 `patch_httpx`
golden-chain/delegation fakes," blocked by OMN-13499, sourced from a shadow-run log filtered to
`patch_httpx`. That does not match live reality in this repo — omniclaude's own detector output
has only **6** `patch_httpx_egress` findings (all in one file), and the "61 pre-existing findings"
language actually lives in the `.pre-commit-config.yaml` comment attributed to OMN-13501/13502,
not OMN-13500. This worklist follows the dispatch brief (61 omniclaude findings, matches live
detector output exactly) rather than the Linear ticket body's 39-site patch_httpx framing, which
appears to be copied from a different ticket/repo context. Reconcile the Linear ticket text before
closing this ticket.

## Cluster assignment legend

- **C1** — routing-boundary `MagicMock` (mock_router / mock_recommendation chain)
- **C2** — skill/task-dispatcher `AsyncMock` (task_dispatcher boundary)
- **C3** — wiring/bridge `MagicMock` (dispatch_engine / quirk_memory_bridge)
- **C4** — inference egress (`patch("httpx.Client")` + `VllmInferenceBackend=MagicMock`)

## Findings

| # | File:Line | Rule | Cluster | Snippet | Planned disposition |
|---|-----------|------|---------|---------|----------------------|
| 1 | tests/lib/core/test_onex_routing_nodes.py:143 | mock_assigned_to_boundary | C1 | `mock_router = MagicMock()` | DONE — replaced `MagicMock()` with typed `FakeAgentRouter` (`tests/support/fake_agent_router.py`); real `route()`/`registry` surface, calls recorded on `route_calls` |
| 2 | tests/lib/core/test_onex_routing_nodes.py:202 | mock_assigned_to_boundary | C1 | `mock_router = MagicMock()` | DONE — replaced `MagicMock()` with typed `FakeAgentRouter` (`tests/support/fake_agent_router.py`); real `route()`/`registry` surface, calls recorded on `route_calls` |
| 3 | tests/lib/core/test_onex_routing_nodes.py:440 | mock_assigned_to_boundary | C1 | `mock_router = MagicMock()` | DONE — replaced `MagicMock()` with typed `FakeAgentRouter` (`tests/support/fake_agent_router.py`); real `route()`/`registry` surface, calls recorded on `route_calls` |
| 4 | tests/lib/core/test_onex_routing_nodes.py:555 | mock_assigned_to_boundary | C1 | `mock_router = MagicMock()` | DONE — replaced `MagicMock()` with typed `FakeAgentRouter` (`tests/support/fake_agent_router.py`); real `route()`/`registry` surface, calls recorded on `route_calls` |
| 5 | tests/lib/core/test_route_via_events_wrapper.py:187 | mock_assigned_to_boundary | C1 | `mock_router = MagicMock()` | DONE — replaced `MagicMock()` with typed `FakeAgentRouter` (`tests/support/fake_agent_router.py`); real `route()`/`registry` surface, calls recorded on `route_calls` |
| 6 | tests/lib/core/test_route_via_events_wrapper.py:218 | mock_assigned_to_boundary | C1 | `mock_router = MagicMock()` | DONE — replaced `MagicMock()` with typed `FakeAgentRouter` (`tests/support/fake_agent_router.py`); real `route()`/`registry` surface, calls recorded on `route_calls` |
| 7 | tests/lib/core/test_route_via_events_wrapper.py:231 | mock_assigned_to_boundary | C1 | `mock_router = MagicMock()` | DONE — replaced `MagicMock()` with typed `FakeAgentRouter` (`tests/support/fake_agent_router.py`); real `route()`/`registry` surface, calls recorded on `route_calls` |
| 8 | tests/lib/core/test_route_via_events_wrapper.py:255 | mock_assigned_to_boundary | C1 | `mock_router = MagicMock()` | DONE — replaced `MagicMock()` with typed `FakeAgentRouter` (`tests/support/fake_agent_router.py`); real `route()`/`registry` surface, calls recorded on `route_calls` |
| 9 | tests/lib/core/test_route_via_events_wrapper.py:284 | mock_assigned_to_boundary | C1 | `mock_router = MagicMock()` | DONE — replaced `MagicMock()` with typed `FakeAgentRouter` (`tests/support/fake_agent_router.py`); real `route()`/`registry` surface, calls recorded on `route_calls` |
| 10 | tests/lib/core/test_route_via_events_wrapper.py:476 | mock_assigned_to_boundary | C1 | `mock_router = MagicMock()` | DONE — replaced `MagicMock()` with typed `FakeAgentRouter` (`tests/support/fake_agent_router.py`); real `route()`/`registry` surface, calls recorded on `route_calls` |
| 11 | tests/lib/core/test_route_via_events_wrapper.py:516 | mock_assigned_to_boundary | C1 | `mock_router = MagicMock()` | DONE — replaced `MagicMock()` with typed `FakeAgentRouter` (`tests/support/fake_agent_router.py`); real `route()`/`registry` surface, calls recorded on `route_calls` |
| 12 | tests/lib/core/test_route_via_events_wrapper.py:542 | mock_assigned_to_boundary | C1 | `mock_router = MagicMock()` | DONE — replaced `MagicMock()` with typed `FakeAgentRouter` (`tests/support/fake_agent_router.py`); real `route()`/`registry` surface, calls recorded on `route_calls` |
| 13 | tests/lib/core/test_route_via_events_wrapper.py:577 | mock_assigned_to_boundary | C1 | `mock_router = MagicMock()` | DONE — replaced `MagicMock()` with typed `FakeAgentRouter` (`tests/support/fake_agent_router.py`); real `route()`/`registry` surface, calls recorded on `route_calls` |
| 14 | tests/lib/core/test_route_via_events_wrapper.py:612 | mock_assigned_to_boundary | C1 | `mock_router = MagicMock()` | DONE — replaced `MagicMock()` with typed `FakeAgentRouter` (`tests/support/fake_agent_router.py`); real `route()`/`registry` surface, calls recorded on `route_calls` |
| 15 | tests/lib/core/test_route_via_events_wrapper.py:647 | mock_assigned_to_boundary | C1 | `mock_router = MagicMock()` | DONE — replaced `MagicMock()` with typed `FakeAgentRouter` (`tests/support/fake_agent_router.py`); real `route()`/`registry` surface, calls recorded on `route_calls` |
| 16 | tests/lib/core/test_route_via_events_wrapper.py:699 | mock_assigned_to_boundary | C1 | `mock_router = MagicMock()` | DONE — replaced `MagicMock()` with typed `FakeAgentRouter` (`tests/support/fake_agent_router.py`); real `route()`/`registry` surface, calls recorded on `route_calls` |
| 17 | tests/lib/core/test_route_via_events_wrapper.py:721 | mock_assigned_to_boundary | C1 | `mock_router = MagicMock()` | DONE — replaced `MagicMock()` with typed `FakeAgentRouter` (`tests/support/fake_agent_router.py`); real `route()`/`registry` surface, calls recorded on `route_calls` |
| 18 | tests/lib/core/test_route_via_events_wrapper.py:750 | mock_assigned_to_boundary | C1 | `mock_router = MagicMock()` | DONE — replaced `MagicMock()` with typed `FakeAgentRouter` (`tests/support/fake_agent_router.py`); real `route()`/`registry` surface, calls recorded on `route_calls` |
| 19 | tests/lib/core/test_route_via_events_wrapper.py:1002 | mock_assigned_to_boundary | C1 | `mock_router = MagicMock()` | DONE — replaced `MagicMock()` with typed `FakeAgentRouter` (`tests/support/fake_agent_router.py`); real `route()`/`registry` surface, calls recorded on `route_calls` |
| 20 | tests/lib/core/test_route_via_events_wrapper.py:1026 | mock_assigned_to_boundary | C1 | `mock_router = MagicMock()` | DONE — replaced `MagicMock()` with typed `FakeAgentRouter` (`tests/support/fake_agent_router.py`); real `route()`/`registry` surface, calls recorded on `route_calls` |
| 21 | tests/lib/core/test_route_via_events_wrapper.py:1064 | mock_assigned_to_boundary | C1 | `mock_router = MagicMock()` | DONE — replaced `MagicMock()` with typed `FakeAgentRouter` (`tests/support/fake_agent_router.py`); real `route()`/`registry` surface, calls recorded on `route_calls` |
| 22 | tests/lib/core/test_route_via_events_wrapper.py:1107 | mock_assigned_to_boundary | C1 | `mock_router = MagicMock()` | DONE — replaced `MagicMock()` with typed `FakeAgentRouter` (`tests/support/fake_agent_router.py`); real `route()`/`registry` surface, calls recorded on `route_calls` |
| 23 | tests/lib/core/test_route_via_events_wrapper.py:1207 | mock_assigned_to_boundary | C1 | `mock_router = MagicMock()` | DONE — replaced `MagicMock()` with typed `FakeAgentRouter` (`tests/support/fake_agent_router.py`); real `route()`/`registry` surface, calls recorded on `route_calls` |
| 24 | tests/lib/core/test_route_via_events_wrapper.py:1307 | mock_assigned_to_boundary | C1 | `mock_router = MagicMock()` | DONE — replaced `MagicMock()` with typed `FakeAgentRouter` (`tests/support/fake_agent_router.py`); real `route()`/`registry` surface, calls recorded on `route_calls` |
| 25 | tests/lib/core/test_routing_event_client.py:461 | mock_assigned_to_boundary | C1 | `mock_router = MagicMock()` | DONE — replaced `MagicMock()` with typed `FakeAgentRouter` (`tests/support/fake_agent_router.py`); real `route()`/`registry` surface, calls recorded on `route_calls` |
| 26 | tests/lib/core/test_routing_event_client.py:546 | mock_assigned_to_boundary | C1 | `mock_router = MagicMock()` | DONE — replaced `MagicMock()` with typed `FakeAgentRouter` (`tests/support/fake_agent_router.py`); real `route()`/`registry` surface, calls recorded on `route_calls` |
| 27 | tests/lib/core/test_routing_event_client.py:624 | mock_assigned_to_boundary | C1 | `mock_router = MagicMock()` | DONE — replaced `MagicMock()` with typed `FakeAgentRouter` (`tests/support/fake_agent_router.py`); real `route()`/`registry` surface, calls recorded on `route_calls` |
| 28 | tests/unit/nodes/node_agent_router/test_handler_agent_router.py:94 | mock_assigned_to_boundary | C1 | `mock_router = MagicMock()` | DONE — replaced `MagicMock()` with typed `FakeAgentRouter` (`tests/support/fake_agent_router.py`); real `route()`/`registry` surface, calls recorded on `route_calls` |
| 29 | tests/unit/nodes/node_agent_router/test_handler_agent_router.py:121 | mock_assigned_to_boundary | C1 | `mock_router = MagicMock()` | DONE — replaced `MagicMock()` with typed `FakeAgentRouter` (`tests/support/fake_agent_router.py`); real `route()`/`registry` surface, calls recorded on `route_calls` |
| 30 | tests/unit/nodes/node_agent_router/test_handler_agent_router.py:143 | mock_assigned_to_boundary | C1 | `mock_router = MagicMock()` | DONE — replaced `MagicMock()` with typed `FakeAgentRouter` (`tests/support/fake_agent_router.py`); real `route()`/`registry` surface, calls recorded on `route_calls` |
| 31 | tests/unit/nodes/node_agent_router/test_handler_agent_router.py:156 | mock_assigned_to_boundary | C1 | `mock_router = MagicMock()` | DONE — replaced `MagicMock()` with typed `FakeAgentRouter` (`tests/support/fake_agent_router.py`); real `route()`/`registry` surface, calls recorded on `route_calls` |
| 32 | tests/unit/nodes/node_agent_router/test_handler_agent_router.py:179 | mock_assigned_to_boundary | C1 | `mock_router = MagicMock()` | DONE — replaced `MagicMock()` with typed `FakeAgentRouter` (`tests/support/fake_agent_router.py`); real `route()`/`registry` surface, calls recorded on `route_calls` |
| 33 | tests/unit/nodes/node_agent_router/test_handler_agent_router.py:200 | mock_assigned_to_boundary | C1 | `mock_router = MagicMock()` | DONE — replaced `MagicMock()` with typed `FakeAgentRouter` (`tests/support/fake_agent_router.py`); real `route()`/`registry` surface, calls recorded on `route_calls` |
| 34 | tests/unit/nodes/node_agent_router/test_handler_agent_router.py:229 | mock_assigned_to_boundary | C1 | `mock_router = MagicMock()` | DONE — replaced `MagicMock()` with typed `FakeAgentRouter` (`tests/support/fake_agent_router.py`); real `route()`/`registry` surface, calls recorded on `route_calls` |
| 35 | tests/unit/nodes/shared/test_handler_skill_requested.py:126 | mock_assigned_to_boundary | C2 | `dispatcher = AsyncMock(return_value=output_with_result)` | DONE — replaced `AsyncMock` with typed `FakeTaskDispatcher` (`tests/support/fake_task_dispatcher.py`); real `async __call__(prompt)->str` returns the parser test-vector and records prompts on `prompts`/`last_prompt`. Recorded-replay harness (`RecordedReplayInferenceTransport`) is httpx-shaped and NOT injectable as the `Callable[[str],Awaitable[str]]` `task_dispatcher` seam; this handler crosses no httpx boundary and the scenarios are synthetic parser vectors (missing/unrecognized/trailing-noise RESULT blocks) not recordable from a live model. |
| 36 | tests/unit/nodes/shared/test_handler_skill_requested.py:140 | mock_assigned_to_boundary | C2 | `dispatcher = AsyncMock(side_effect=RuntimeError("connection refused"))` | DONE — replaced `AsyncMock(side_effect=...)` with `FakeTaskDispatcher(error=RuntimeError(...))`; the real `__call__` records the prompt then raises, exercising the handler's `except Exception` path (FAILED result + `error_type="RuntimeError"`) through real callable execution — a plausible inference-backend failure (refused connection), not mock `side_effect` machinery. |
| 37 | tests/unit/nodes/shared/test_handler_skill_requested.py:156 | mock_assigned_to_boundary | C2 | `dispatcher = AsyncMock(return_value=polly_output)` | DONE — replaced `AsyncMock` with typed `FakeTaskDispatcher` (`tests/support/fake_task_dispatcher.py`); real `async __call__(prompt)->str` returns the parser test-vector and records prompts on `prompts`/`last_prompt`. Recorded-replay harness (`RecordedReplayInferenceTransport`) is httpx-shaped and NOT injectable as the `Callable[[str],Awaitable[str]]` `task_dispatcher` seam; this handler crosses no httpx boundary and the scenarios are synthetic parser vectors (missing/unrecognized/trailing-noise RESULT blocks) not recordable from a live model. |
| 38 | tests/unit/nodes/shared/test_handler_skill_requested.py:171 | mock_assigned_to_boundary | C2 | `dispatcher = AsyncMock(return_value="Done, but no structured block here.")` | DONE — replaced `AsyncMock` with typed `FakeTaskDispatcher` (`tests/support/fake_task_dispatcher.py`); real `async __call__(prompt)->str` returns the parser test-vector and records prompts on `prompts`/`last_prompt`. Recorded-replay harness (`RecordedReplayInferenceTransport`) is httpx-shaped and NOT injectable as the `Callable[[str],Awaitable[str]]` `task_dispatcher` seam; this handler crosses no httpx boundary and the scenarios are synthetic parser vectors (missing/unrecognized/trailing-noise RESULT blocks) not recordable from a live model. |
| 39 | tests/unit/nodes/shared/test_handler_skill_requested.py:191 | mock_assigned_to_boundary | C2 | `dispatcher = AsyncMock(return_value=polly_output)` | DONE — replaced `AsyncMock` with typed `FakeTaskDispatcher` (`tests/support/fake_task_dispatcher.py`); real `async __call__(prompt)->str` returns the parser test-vector and records prompts on `prompts`/`last_prompt`. Recorded-replay harness (`RecordedReplayInferenceTransport`) is httpx-shaped and NOT injectable as the `Callable[[str],Awaitable[str]]` `task_dispatcher` seam; this handler crosses no httpx boundary and the scenarios are synthetic parser vectors (missing/unrecognized/trailing-noise RESULT blocks) not recordable from a live model. |
| 40 | tests/unit/nodes/shared/test_handler_skill_requested.py:203 | mock_assigned_to_boundary | C2 | `dispatcher = AsyncMock(return_value=output)` | DONE — replaced `AsyncMock` with typed `FakeTaskDispatcher` (`tests/support/fake_task_dispatcher.py`); real `async __call__(prompt)->str` returns the parser test-vector and records prompts on `prompts`/`last_prompt`. Recorded-replay harness (`RecordedReplayInferenceTransport`) is httpx-shaped and NOT injectable as the `Callable[[str],Awaitable[str]]` `task_dispatcher` seam; this handler crosses no httpx boundary and the scenarios are synthetic parser vectors (missing/unrecognized/trailing-noise RESULT blocks) not recordable from a live model. |
| 41 | tests/unit/nodes/shared/test_handler_skill_requested.py:220 | mock_assigned_to_boundary | C2 | `dispatcher = AsyncMock(return_value=polly_output)` | DONE — replaced `AsyncMock` with typed `FakeTaskDispatcher` (`tests/support/fake_task_dispatcher.py`); real `async __call__(prompt)->str` returns the parser test-vector and records prompts on `prompts`/`last_prompt`. Recorded-replay harness (`RecordedReplayInferenceTransport`) is httpx-shaped and NOT injectable as the `Callable[[str],Awaitable[str]]` `task_dispatcher` seam; this handler crosses no httpx boundary and the scenarios are synthetic parser vectors (missing/unrecognized/trailing-noise RESULT blocks) not recordable from a live model. |
| 42 | tests/unit/nodes/shared/test_handler_skill_requested.py:249 | mock_assigned_to_boundary | C2 | `dispatcher = AsyncMock(return_value=polly_output)` | DONE — replaced `AsyncMock` with typed `FakeTaskDispatcher` (`tests/support/fake_task_dispatcher.py`); real `async __call__(prompt)->str` returns the parser test-vector and records prompts on `prompts`/`last_prompt`. Recorded-replay harness (`RecordedReplayInferenceTransport`) is httpx-shaped and NOT injectable as the `Callable[[str],Awaitable[str]]` `task_dispatcher` seam; this handler crosses no httpx boundary and the scenarios are synthetic parser vectors (missing/unrecognized/trailing-noise RESULT blocks) not recordable from a live model. |
| 43 | tests/unit/nodes/shared/test_handler_skill_requested.py:306 | mock_assigned_to_boundary | C2 | `dispatcher = AsyncMock(return_value="RESULT:\nstatus: success\nerror:\n")` | DONE — replaced `AsyncMock` with typed `FakeTaskDispatcher` (`tests/support/fake_task_dispatcher.py`); real `async __call__(prompt)->str` returns the parser test-vector and records prompts on `prompts`/`last_prompt`. Recorded-replay harness (`RecordedReplayInferenceTransport`) is httpx-shaped and NOT injectable as the `Callable[[str],Awaitable[str]]` `task_dispatcher` seam; this handler crosses no httpx boundary and the scenarios are synthetic parser vectors (missing/unrecognized/trailing-noise RESULT blocks) not recordable from a live model. |
| 44 | tests/unit/nodes/shared/test_handler_skill_requested.py:332 | mock_assigned_to_boundary | C2 | `dispatcher = AsyncMock(side_effect=RuntimeError("dispatch failed"))` | DONE — replaced `AsyncMock(side_effect=...)` with `FakeTaskDispatcher(error=RuntimeError(...))`; the real `__call__` records the prompt then raises, exercising the handler's `except Exception` path (FAILED result + `error_type="RuntimeError"`) through real callable execution — a plausible inference-backend failure (refused connection), not mock `side_effect` machinery. |
| 45 | tests/unit/nodes/shared/test_handler_skill_requested.py:352 | mock_assigned_to_boundary | C2 | `dispatcher = AsyncMock(return_value="RESULT:\nstatus: success\nerror:\n")` | DONE — replaced `AsyncMock` with typed `FakeTaskDispatcher` (`tests/support/fake_task_dispatcher.py`); real `async __call__(prompt)->str` returns the parser test-vector and records prompts on `prompts`/`last_prompt`. Recorded-replay harness (`RecordedReplayInferenceTransport`) is httpx-shaped and NOT injectable as the `Callable[[str],Awaitable[str]]` `task_dispatcher` seam; this handler crosses no httpx boundary and the scenarios are synthetic parser vectors (missing/unrecognized/trailing-noise RESULT blocks) not recordable from a live model. |
| 46 | tests/unit/nodes/shared/test_handler_skill_requested.py:367 | mock_assigned_to_boundary | C2 | `dispatcher = AsyncMock(return_value="RESULT:\nstatus: success\nerror:\n")` | DONE — replaced `AsyncMock` with typed `FakeTaskDispatcher` (`tests/support/fake_task_dispatcher.py`); real `async __call__(prompt)->str` returns the parser test-vector and records prompts on `prompts`/`last_prompt`. Recorded-replay harness (`RecordedReplayInferenceTransport`) is httpx-shaped and NOT injectable as the `Callable[[str],Awaitable[str]]` `task_dispatcher` seam; this handler crosses no httpx boundary and the scenarios are synthetic parser vectors (missing/unrecognized/trailing-noise RESULT blocks) not recordable from a live model. |
| 47 | tests/unit/nodes/shared/test_handler_skill_requested.py:388 | mock_assigned_to_boundary | C2 | `dispatcher = AsyncMock(return_value="RESULT:\nstatus: success\nerror:\n")` | DONE — replaced `AsyncMock` with typed `FakeTaskDispatcher` (`tests/support/fake_task_dispatcher.py`); real `async __call__(prompt)->str` returns the parser test-vector and records prompts on `prompts`/`last_prompt`. Recorded-replay harness (`RecordedReplayInferenceTransport`) is httpx-shaped and NOT injectable as the `Callable[[str],Awaitable[str]]` `task_dispatcher` seam; this handler crosses no httpx boundary and the scenarios are synthetic parser vectors (missing/unrecognized/trailing-noise RESULT blocks) not recordable from a live model. |
| 48 | tests/unit/nodes/shared/test_handler_skill_requested.py:412 | mock_assigned_to_boundary | C2 | `dispatcher = AsyncMock(return_value="RESULT:\nstatus: success\nerror:\n")` | DONE — replaced `AsyncMock` with typed `FakeTaskDispatcher` (`tests/support/fake_task_dispatcher.py`); real `async __call__(prompt)->str` returns the parser test-vector and records prompts on `prompts`/`last_prompt`. Recorded-replay harness (`RecordedReplayInferenceTransport`) is httpx-shaped and NOT injectable as the `Callable[[str],Awaitable[str]]` `task_dispatcher` seam; this handler crosses no httpx boundary and the scenarios are synthetic parser vectors (missing/unrecognized/trailing-noise RESULT blocks) not recordable from a live model. |
| 49 | tests/unit/nodes/shared/test_handler_skill_requested.py:440 | mock_assigned_to_boundary | C2 | `dispatcher = AsyncMock(return_value="RESULT:\nstatus: success\nerror:\n")` | DONE — replaced `AsyncMock` with typed `FakeTaskDispatcher` (`tests/support/fake_task_dispatcher.py`); real `async __call__(prompt)->str` returns the parser test-vector and records prompts on `prompts`/`last_prompt`. Recorded-replay harness (`RecordedReplayInferenceTransport`) is httpx-shaped and NOT injectable as the `Callable[[str],Awaitable[str]]` `task_dispatcher` seam; this handler crosses no httpx boundary and the scenarios are synthetic parser vectors (missing/unrecognized/trailing-noise RESULT blocks) not recordable from a live model. |
| 50 | tests/unit/nodes/shared/test_handler_skill_requested.py:465 | mock_assigned_to_boundary | C2 | `dispatcher = AsyncMock(return_value="RESULT:\nstatus: success\nerror:\n")` | DONE — replaced `AsyncMock` with typed `FakeTaskDispatcher` (`tests/support/fake_task_dispatcher.py`); real `async __call__(prompt)->str` returns the parser test-vector and records prompts on `prompts`/`last_prompt`. Recorded-replay harness (`RecordedReplayInferenceTransport`) is httpx-shaped and NOT injectable as the `Callable[[str],Awaitable[str]]` `task_dispatcher` seam; this handler crosses no httpx boundary and the scenarios are synthetic parser vectors (missing/unrecognized/trailing-noise RESULT blocks) not recordable from a live model. |
| 51 | tests/unit/runtime/test_wiring_dispatchers.py:724 | mock_assigned_to_boundary | C3 | `config.dispatch_engine = MagicMock()` | DONE — replaced `MagicMock` config with typed `_StubWireDispatchersConfig` (real `MessageDispatchEngine()`, typed `_StubContainer`, real `UUID`). The skip path DOES invoke the engine (`wire_quirk_finding_subscription` registers a dispatcher + route), so a read-only stub would break; the real engine handles the registration and the assertion (`resources_created == []`, message "skipped") still holds because no skill contracts load. |
| 52 | tests/unit/runtime/test_wiring_dispatchers.py:824 | mock_assigned_to_boundary | C3 | `mock_bridge = MagicMock()` | DONE — replaced with a real in-memory bridge `NodeQuirkMemoryBridgeEffect(store=StorePatternInMemory())` fed a serialized valid `QuirkFinding` payload; asserts real promotion side effect (pattern persisted under `QUIRK:STUB_CODE:OBSERVE`, finding_id in `evidence_bundle_ids`). Not an inference boundary. |
| 53 | tests/unit/runtime/test_wiring_dispatchers.py:866 | mock_assigned_to_boundary | C3 | `mock_bridge = MagicMock()` | DONE — real `NodeQuirkMemoryBridgeEffect` + `StorePatternInMemory`; a genuinely malformed payload (`{"bad": "data"}`) fails `QuirkFinding.model_validate` inside `process_payload`, which returns `None` → dispatcher returns `None`. Asserts store stayed empty. |
| 54 | tests/unit/runtime/test_wiring_dispatchers.py:888 | mock_assigned_to_boundary | C3 | `mock_bridge = MagicMock()` | DONE — real `NodeQuirkMemoryBridgeEffect` over typed Protocol-conformant `_RaisingPatternStore` (`ProtocolPatternStore` shape, raises on `get_by_key`). A valid finding parses, `promote_finding` hits the raising store, the error escapes `process_payload`, and the dispatcher's real fail-open path swallows it → `None`. Exercises the actual exception path instead of a `side_effect`. |
| 55 | tests/runtime/test_lifecycle.py:128 | mock_assigned_to_boundary | C4 | `VllmInferenceBackend=MagicMock(return_value=MagicMock())` | DONE — typed wiring stub `_StubVllmBackend` (real class, `registry=` kwarg + `aclose`). Triaged: `on_start` only CONSTRUCTS `VllmInferenceBackend(registry=...)` and stores it on `state.vllm_backend`; it never calls `infer`/`chat_completion_sync`, so no inference egress runs — a typed no-I/O double is correct (not recorded-replay). |
| 56 | tests/unit/nodes/node_local_llm_inference_effect/test_backend_vllm_tools.py:193 (`test_text_response`) | patch_httpx_egress | C4 | `with patch("httpx.Client") as mock_client_cls:` | DONE — canonical `RecordedReplayInferenceTransport` replaying a REAL fixture (`tests/fixtures/golden_chain/vllm_chat_text.json`) recorded from live vLLM (Qwen3.6-35B-A3B on .201:8000). Backend constructs the request LIVE; transport returns bytes only on endpoint+request_hash+model match. `patch("httpx.Client", return_value=transport)` is the detector-exempt canonical seam. |
| 57 | tests/unit/nodes/node_local_llm_inference_effect/test_backend_vllm_tools.py:220 (`test_tool_call_response`) | patch_httpx_egress | C4 | `with patch("httpx.Client") as mock_client_cls:` | DONE — `RecordedReplayInferenceTransport` replaying a REAL recorded tool-call response (`vllm_chat_tool_call.json`; model emitted content + a real `read_file` tool call, so the harness's non-empty-completion guard is satisfied). |
| 58 | tests/unit/nodes/node_local_llm_inference_effect/test_backend_vllm_tools.py:239 (`test_timeout_returns_error`) | patch_httpx_egress | C4 | `with patch("httpx.Client") as mock_client_cls:` | DONE — REAL `httpx.Client` driven by `httpx.MockTransport` (raises `httpx.ReadTimeout`) via a new backend `sync_transport` injection seam. Real request serialization runs; recorded-replay cannot induce a transport timeout, so this transport-mechanics path is not an inference-content fake. No `patch("httpx.Client")`, no MagicMock — detector-clean. |
| 59 | tests/unit/nodes/node_local_llm_inference_effect/test_backend_vllm_tools.py:256 (`test_network_error...`) | patch_httpx_egress | C4 | `with patch("httpx.Client") as mock_client_cls:` | DONE — REAL `httpx.Client` + `httpx.MockTransport` raising `httpx.ConnectError` via the `sync_transport` seam; proves `BACKEND_UNAVAILABLE` mapping. |
| 60 | tests/unit/nodes/node_local_llm_inference_effect/test_backend_vllm_tools.py:274 (`test_non_200...`) | patch_httpx_egress | C4 | `with patch("httpx.Client") as mock_client_cls:` | DONE — REAL `httpx.Client` + `httpx.MockTransport` returning `httpx.Response(503,...)` via the `sync_transport` seam; proves the HTTP-status error path. |
| 61 | tests/unit/nodes/node_local_llm_inference_effect/test_backend_vllm_tools.py:294 (`test_tools_and_tool_choice...`) | patch_httpx_egress | C4 | `with patch("httpx.Client") as mock_client_cls:` | DONE — REAL `httpx.Client` + `httpx.MockTransport` whose handler CAPTURES the serialized outgoing request; asserts URL + tools/tool_choice/max_tokens/temperature on the actually-serialized body (stronger than the old `call_args` inspection). New drift-proof test `test_wrong_model_fails_replay` also added (wrong model → REQUEST_HASH_MISMATCH). |

## Cluster sizing summary

| Cluster | Findings | Files | Fix strategy |
|---------|----------|-------|---------------|
| C1 — routing-boundary MagicMock | 34 (**DONE 2026-07-03**) | test_onex_routing_nodes.py, test_route_via_events_wrapper.py, test_routing_event_client.py, test_handler_agent_router.py | These tests stub the deterministic keyword/trigger-matching agent router (`mock_router.route()`, `.registry`), not an LLM boundary. Replace `MagicMock()` with a real router instance constructed against a minimal-but-real registry dict, or (where full construction needs unavailable IO) a typed contract-level fake implementing the router's real Protocol. No recorded-replay needed — routing here is non-inference. **Resolution:** added a real typed `FakeAgentRouter` (`tests/support/fake_agent_router.py`) implementing the router's real `route()`/`registry` call surface (positional or kw args, records calls on `route_calls`); all 34 `mock_router = MagicMock()` sites now build `FakeAgentRouter`. Detector 61→27; all 127 affected tests green. |
| C2 — skill/task-dispatcher AsyncMock | 16 (**DONE 2026-07-03**) | test_handler_skill_requested.py | `dispatcher`/`task_dispatcher` is injected into `handle_skill_requested` and matches the real contract `TaskDispatcher = Callable[[str], Awaitable[str]]`. In production (`runtime/wiring_dispatchers.py`) it is a thin closure ABOVE the concrete inference backend (`cc_backend.session_query` / `vllm_backend.infer`) — a contract seam, not the httpx inference boundary. **Evidence that recorded-replay does NOT fit here:** the canonical harness `omnibase_core.runtime.golden_chain.RecordedReplayInferenceTransport` is `httpx.Client`-shaped (`.post(url, json=payload)`), cannot be injected as a str→str callable, and `handle_skill_requested` crosses no httpx boundary (the httpx egress lives inside the backends, which this shared-handler unit deliberately does not stand up). The scenarios are synthetic parser vectors (missing RESULT block, unrecognized status, trailing noise after blank line) that a live model cannot be made to emit on demand, so `OMN_RECORD_GOLDEN=1` recording is not meaningful. Per the detector's `mock_assigned_to_boundary` remedy → typed contract-level fake. **Resolution:** added real typed `FakeTaskDispatcher` (`tests/support/fake_task_dispatcher.py`) with a real `async __call__(prompt)->str` returning the configured parser vector and recording prompts (`prompts`/`last_prompt`/`await_count`); error-path variant (`error=`) records then raises a real backend-shaped failure. All 16 sites migrated; detector 27→11; 22/22 tests pass. Recorded-replay remains the correct tool for C4 (the vLLM httpx egress). |
| C3 — wiring/bridge MagicMock | 4 (**DONE 2026-07-03**) | test_wiring_dispatchers.py | Two distinct boundaries: (a) `config.dispatch_engine = MagicMock()` — replaced with typed `_StubWireDispatchersConfig` carrying only the fields `wire_dispatchers` reads, backed by a **real** `MessageDispatchEngine()` (correction: the skip path DOES invoke the engine via `wire_quirk_finding_subscription`, so a read-only stub was infeasible; the real engine handles registration and the skip assertion still holds because no contracts load); (b) `mock_bridge` (x3) for `QuirkMemoryBridge.process_payload` — replaced with a **real** `NodeQuirkMemoryBridgeEffect(store=StorePatternInMemory())` for the promote (valid payload → real persistence assertion) and parse-error (malformed payload → real `None`) cases, and a typed `ProtocolPatternStore`-conformant `_RaisingPatternStore` for the fail-open case (real bridge exception propagates and is swallowed). Detector on this file: 4→0; repo-wide 11→7 (the remaining 7 are all C4 `patch_httpx_egress`/backend `MagicMock` in test_backend_vllm_tools.py + test_lifecycle.py). All 47 tests in file green; no new mypy errors introduced (3 pre-existing errors in untouched `_make_contract`/line-442 code). |
| C4 — inference egress (patch_httpx + backend MagicMock) | 7 (**DONE 2026-07-03**) | test_backend_vllm_tools.py (6), test_lifecycle.py (1) | Split by what each site proves. **Inference CONTENT (2):** `test_text_response` + `test_tool_call_response` migrated to the canonical `RecordedReplayInferenceTransport` (OMN-13499) replaying REAL bytes recorded once from live vLLM (`Qwen3.6-35B-A3B` on `.201:8000` via Tailscale `100.109.203.94`) into `tests/fixtures/golden_chain/vllm_chat_{text,tool_call}.json`. Backend builds the request LIVE; transport returns bytes only on endpoint+request_hash+model match (`patch("httpx.Client", return_value=transport)` is the detector-exempt canonical seam). Added `test_wrong_model_fails_replay` proving a drifted model → REQUEST_HASH_MISMATCH (replay is evidence, not authority). **Transport MECHANICS (4):** `test_timeout`/`test_network_error`/`test_non_200`/`test_tools_...payload` converted to a REAL `httpx.Client` driven by `httpx.MockTransport`, injected through a new backend `sync_transport` seam (idiomatic httpx transport injection; additive, backward-compatible kwarg). These prove HTTP error mapping + request serialization — NOT inference content — which recorded-replay (recorded 2xx bytes only) structurally cannot express; no `patch("httpx.Client")` / MagicMock remains, so they are detector-clean without any allow marker. **Wiring (1):** `test_lifecycle.py:128` — `on_start` only CONSTRUCTS the backend (never invokes it), so replaced the MagicMock with a typed no-I/O `_StubVllmBackend`. Detector on both files: 7→0; repo-wide 7→0. Fixtures recorded with `OMN_RECORD_GOLDEN=1`; replay is fully offline in CI. Zero `# onex-allow-faked-boundary` markers used. |

## Recommended fix-group dispatch (max 4 agents)

1. **Agent A — C1 (34 findings, 4 files):** mechanical, single-pattern migration (`mock_router` → real/typed router). Largest count but lowest risk/complexity per site; batchable.
2. **Agent B — C2 (16 findings, 1 file):** recorded-replay migration for the skill-dispatch boundary; requires OMN-13499 harness import + fixture recording session.
3. **Agent C — C3 (4 findings, 1 file):** smallest cluster; typed-fake/config-narrowing, no recorded-replay needed.
4. **Agent D — C4 (7 findings, 2 files):** recorded-replay migration for the vLLM httpx boundary; requires live-endpoint recording session (same harness as Agent B, different fixture set).

## OCC deploy evidence

The only runtime source change in this burn-down is a backward-compatible `sync_transport`
dependency-injection seam in `src/omniclaude/nodes/node_local_llm_inference_effect/backends/backend_vllm.py`
(default `None` preserves production behavior). Because it touches a runtime path, `deploy-gate` requires
deploy DoD evidence in an OCC contract. Companion OCC PR **onex_change_control#3552** carries net-new
`contracts/OMN-13500.yaml` plus `dod-omniclaude-pr-1849`, `dod-deploy-assessment` (deploy disposition:
runtime path touched, no live mutation, operator-gated), and `dod-occ-self` receipts, all
`contract_sha256`-bound (`sha256:93d5bbf3…`). This PR pins `Evidence-Source: OCC#3552` +
`Evidence-Ticket: OMN-13500` so `deploy-gate`, `occ-preflight`, and the Receipt Gate resolve the contract
from that OCC PR head.
