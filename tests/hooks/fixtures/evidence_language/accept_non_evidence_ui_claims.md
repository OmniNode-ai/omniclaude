# Architecture Overview

This document describes how UI POST requests are processed.

end-to-end flow:

1. UI POST arrives at the ingress node
2. Handler processes the event envelope
3. Projection updates the dashboard

click here to learn more: https://docs.example.com

This is NOT an evidence doc — it is in the wrong path for the hook to fire.
