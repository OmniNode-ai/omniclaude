agent(
  `File the concurrent-lane directory-clobbering ticket (second occurrence today — the friction rule mandates it). SEARCH LINEAR FIRST for existing coverage (worktree hygiene / cleanup discipline); adopt with evidence if found.

THE TWO INCIDENTS (2026-08-31, both disclosed by the agents themselves): (1) an egress lane ran 'rm -rf' on the shared ticket parent dir omni_worktrees/OMN-17372/ during cleanup, deleting ANOTHER lane's stale worktree registration (the strip lane's; its live worktree + origin branch survived, no committed work lost, but uncommitted files in the deleted dir cannot be ruled out); (2) a registry-corrections lane's scratch clone at omni_home/_kbi_sweep/kbi was deleted by a concurrent lane mid-work, orphaning its worktree after push (commit already on remote, nothing lost, forced a re-clone + full gate re-run).

FILE (team Omninode, "Sprint 2026-09-07 → 2026-09-13", High): the defect class is lanes deleting directories they do not own during cleanup, under multi-lane concurrency that is now the norm (20+ lanes/day). Fix shape to propose (enforcement, not doctrine): (a) rule — a lane removes ONLY worktrees it created, via 'git worktree remove', never 'rm -rf'; never a shared ticket parent dir; scratch dirs carry the creating run-id in the path and only that run deletes them; (b) a guard hook rejecting 'rm -rf' against omni_worktrees/* parent dirs or another run's scratch namespace (check what the existing worktree-guard/canonical-clone-guard hooks can be extended with — name the file); (c) the worktree-prune skill remains the ONLY cross-lane cleanup authority. Acceptance: the guard demonstrably blocks the two incident shapes. Cite both incidents with their lane/PR context. Return: ticket id (or adopted id + comment), one-line disposition.`,
  { label: 'file:worktree-clobber', effort: 'low' },
)
