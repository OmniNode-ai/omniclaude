agent(`Small Linear-only lane. The operator ruled (paraphrase, never quote) that the four escalated omnicursor decisions resolve mechanically from existing doctrine rather than needing operator rulings. Apply the resolutions:

1. OMN-16596 (Cursor-native layout migration, currently Backlog "pending scope ack"): move it into the sprint project "Sprint 2026-09-07 → 2026-09-13"; comment that the re-measured scope (~293 occurrences / 94 files) is acknowledged as information — the migration direction was already approved 2026-08-26 — and that the repo-loses-own-hooks side effect is the implementer's judgment call, with a stated preference for a small checked-in dev shim, to be documented in the PR body either way.
2. OMN-16598 (B5 measurement parity, currently Backlog "pending phase ruling"): move into the same sprint project; comment the resolution: phase stays "implement" by the parity rule — the donor (omniclaude) emits phase metrics only from pipeline instrumentation and its own unknown-phase convention defaults to IMPLEMENT (three call sites), so the interactive Cursor emission uses the donor's unknown-phase default; and the cmd-leg tier stays telemetry because omnicursor.yaml's header commits to mirroring the claude registry's tiers for shared keys and the donor's equivalent legs are telemetry. Neither required an operator ruling; both derive from the already-accepted parity framework.
3. OMN-16601 (the UNKNOWN-enum proposal): comment that it does NOT gate OMN-16598 — it continues as its own proposal, and if ever pursued it must flip BOTH platforms (claude + cursor emitters and the enum) in one deliberate change; add a relation link to OMN-16598 if the API allows.
4. OMN-16599 (the tier question's home): comment that the cursor cmd leg shipped/stays telemetry per registry mirroring, that any durability promotion is a cross-platform decision belonging to this ticket, and that the actionable near-term piece is spool-drop observability metrics rather than tier promotion.

Professional register, no operator quotes, no other mutations. RETURN: updated (ticket ids with what changed), notes.`, { label: 'deescalate-tickets', phase: 'Update tickets', schema: SCHEMA })
