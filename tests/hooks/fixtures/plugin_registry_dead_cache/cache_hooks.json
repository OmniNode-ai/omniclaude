{
  "$schema": "https://claude.ai/schemas/hooks.json",
  "description": "DEAD CACHE fixture [OMN-15274]. Shape-faithful copy of what ~/.claude/plugins/cache/omninode-tools/onex/<version>/hooks/hooks.json looked like: an older version, an older hook set, and -- the trap -- a SubagentStop registration that the live load path does not carry. Reading this tree answers 'the report-contract guard is deployed'. It is not: this directory has not been read by Claude Code since it was written.",
  "version": "1.3.0",
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "^mcp__linear-server__(save_issue|update_issue)$",
        "hooks": [
          {
            "type": "command",
            "command": "${CLAUDE_PLUGIN_ROOT}/hooks/scripts/pre_tool_use_done_flip_guard.sh"
          }
        ]
      }
    ],
    "SubagentStop": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "${CLAUDE_PLUGIN_ROOT}/hooks/scripts/subagent_stop_report_contract_guard.sh"
          }
        ]
      }
    ]
  }
}
