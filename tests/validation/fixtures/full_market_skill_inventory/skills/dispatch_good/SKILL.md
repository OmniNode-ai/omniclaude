---
description: Dispatches to a real node.
---

# Dispatch Good

Run `uv run onex run node_real -- --ticket TICKET-ID`.

Also references `node_no_contract` and `node_missing` for inventory coverage.
