{
  "$schema": "https://anthropic.com/claude-code/marketplace.schema.json",
  "name": "omninode-tools",
  "version": "2.0.0",
  "description": "OmniNode ONEX delegate-only plugin — single-command local LLM delegation",
  "owner": {
    "name": "OmniNode",
    "email": "dev@omninode.ai"
  },
  "plugins": [
    {
      "name": "onex",
      "description": "ONEX single-command local LLM delegation. Ships exactly one skill (delegate) — no hooks, no agents (OMN-14688).",
      "version": "0.1.0",
      "source": "./onex-delegate",
      "category": "development",
      "homepage": "https://github.com/OmniNode-ai/omniclaude",
      "requires": {
        "onex_cli": {
          "package": "omnibase-core",
          "min_version": "0.39.0",
          "install_hint": "pipx install 'omnibase-core>=0.39.0'",
          "source_of_truth": "./onex-delegate/plugin-compat.yaml#min_runtime_version",
          "notes": "The `onex` console_script ships with omnibase-core (>=0.39.0). Python 3.12+ required. MVP install path is pipx; a self-contained Homebrew tap binary is planned (see plan § 4.4 / BF-2)."
        }
      }
    }
  ]
}
