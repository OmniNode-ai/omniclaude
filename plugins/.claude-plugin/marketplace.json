{
  "$schema": "https://anthropic.com/claude-code/marketplace.schema.json",
  "name": "omninode-tools",
  "version": "2.0.0",
  "description": "OmniNode ONEX delegate-only plugin \u2014 single-command local LLM delegation",
  "owner": {
    "name": "OmniNode",
    "email": "dev@omninode.ai"
  },
  "plugins": [
    {
      "name": "onex",
      "description": "ONEX single-command local LLM delegation. Ships exactly one skill (delegate) \u2014 no hooks, no agents (OMN-14688).",
      "version": "0.1.0",
      "source": "./onex-delegate",
      "category": "development",
      "homepage": "https://github.com/OmniNode-ai/omniclaude",
      "requires": {
        "onex_cli": {
          "package": "omnibase-infra",
          "min_version": "0.38.4",
          "console_script_package": "omnibase-core",
          "console_script_min_version": "0.46.8",
          "install_hint": "uv tool install --with 'omnibase-infra>=0.38.4' 'omnibase-core>=0.46.8'",
          "install_hint_pipx": "pipx install 'omnibase-core>=0.46.8' && pipx inject omnibase-core 'omnibase-infra>=0.38.4'",
          "cwd_independent": true,
          "installable_from_pypi": true,
          "notes": "OMN-16041: `omnibase-core` ships the `onex` console script; the `delegate` SUBCOMMAND is registered by `omnibase-infra` via the `onex.cli` entry-point group. Installing omnibase-core alone yields `Error: No such command 'delegate'` (exit 2). Both packages must be installed into the SAME environment. Installable from PyPI as of 2026-08-15: omnibase-infra 0.38.6 is published, so the declared pins resolve against the live index. Python 3.12+ required.",
          "source_of_truth": "./onex-delegate/plugin-compat.yaml#onex_cli"
        }
      }
    }
  ]
}
