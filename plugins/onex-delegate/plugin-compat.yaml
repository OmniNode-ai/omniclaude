# SPDX-FileCopyrightText: 2025 OmniNode.ai Inc.
# SPDX-License-Identifier: MIT
#
# plugin-compat.yaml — onex-delegate (consumer-facing, delegate-only) plugin
# compatibility matrix.
#
# This is the slim, consumer-distributed plugin's OWN compat surface — separate
# from plugins/onex/plugin-compat.yaml, which remains the compat matrix for the
# full internal/dev skill tree (plugins/onex, not marketplace-distributed as of
# OMN-14688). Both are read independently; do not merge them.
#
# Read by: test_marketplace_cli_pin.py (this plugin's copy, under
# plugins/onex-delegate/tests/) as the source of truth for the `onex` CLI pin
# that plugin.json and marketplace.json must agree with.
#
# OMN-14688 (Phase 1 of epic OMN-14686): minimal stable package.

plugin: onex
plugin_version: "0.1.0"

# Compatibility window: plugin works with runtimes in [min, max).
min_runtime_version: "0.2.0"
max_runtime_version: "1.0.0"

# `onex` CLI pin (OMN-8799, SD-12) — must match plugin.json requires.onex_cli
# and marketplace.json plugins[0].requires.onex_cli.
#
# OMN-16041 CORRECTION. This block previously declared
# `package: omnibase-core, min_version: 0.39.0` with the hint
# `pipx install 'omnibase-core>=0.39.0'`. That was factually wrong and it made
# the plugin's ONLY command un-runnable on a clean machine:
#
#   * `omnibase-core` ships the `onex` CONSOLE SCRIPT
#     (omnibase_core/pyproject.toml [project.scripts] onex = ...cli_commands:cli)
#     and the loader that discovers the `onex.cli` entry-point group.
#   * `omnibase-infra` ships the `delegate` SUBCOMMAND
#     (omnibase_infra/pyproject.toml [project.entry-points."onex.cli"]
#      delegate = omnibase_infra.cli.cli_delegate:delegate_command).
#
#   Installing only omnibase-core therefore yields, verbatim:
#     Error: No such command 'delegate'. Did you mean 'gate'?   (exit 2)
#
# Both packages must be present IN THE SAME environment. `package` names the
# package that provides this plugin's only command; `console_script_package`
# names the package that provides the `onex` executable itself. The keys are
# flat (never nested) because session_start_onex_cli_pin_check.sh parses this
# block with an awk state machine, not a YAML parser.
onex_cli:
  package: "omnibase-infra"
  min_version: "0.38.4"
  console_script_package: "omnibase-core"
  console_script_min_version: "0.46.8"
  # The package that ships the BACKING NODE (OMN-16191). `package` and
  # `console_script_package` between them produce an `onex` with a `delegate`
  # subcommand, but the subcommand dispatches node_delegate_skill_orchestrator,
  # which lives in neither of them. Node lookup is `onex.nodes` entry-point
  # resolution over INSTALLED distributions (omnibase_core cli_node.py
  # _resolve_packaged_contract), so the node is reachable only when omnimarket
  # is installed into the SAME environment. Omitting it produced, verbatim:
  #   Error: Unknown node 'node_delegate_skill_orchestrator'
  # on a byte-for-byte-as-documented clean install — with ~130 other nodes
  # listed as "known", which made it read like a registry bug rather than a
  # missing package. There is no workspace-root requirement here at all: the
  # entry point resolves out of site-packages.
  node_package: "omnimarket"
  node_package_min_version: "0.4.7"
  # CORRECTED 2026-08-25 (OMN-16528, live-reproduced). The previous hints here
  # pinned `omnimarket>=0.4.7` from PyPI. `onex delegate` runs a pre-flight
  # drift guard (omnibase_infra.cli.omnimarket_drift_guard, OMN-13930) before
  # every REAL dispatch (not `--help`, which click answers before any guard
  # runs): on any machine that has a canonical `omnimarket` clone checked out
  # under the workspace root that guard reads -- true for every OmniNode dev
  # workspace, not for a customer -- it REJECTS an `omnimarket` install that
  # is not a git-VCS install matching that clone's checked-out commit exactly.
  # `omnimarket>=0.4.7` (PyPI) carries no VCS provenance
  # (installed_omnimarket_commit() reads None for it) and fails this check by
  # construction, every time -- not intermittently. The hints below therefore
  # install `omnimarket` as a git+URL direct reference rather than a PyPI pin.
  #
  # CORRECTED AGAIN 2026-08-31 (OMN-16855). Until now the ref was resolved
  # in-line by expanding a braced OMNI_HOME reference with a `.` fallback and
  # running `git -C <that>/omnimarket rev-parse HEAD 2>/dev/null || echo dev`.
  # On any machine without that variable -- i.e. every customer -- the
  # expansion resolved to the CALLER'S CURRENT WORKING DIRECTORY, the
  # `rev-parse` failed there, and the `|| echo dev` swallowed it: the customer
  # installed an unpinned `dev` ref from a command that reads like a commit
  # pin. Silent in both directions, and the no-monorepo-refs gate could not
  # see it either, because its single pattern matched only the bare `$`-form
  # (OMN-16850, now widened).
  #
  # The published hints below name NO workspace directory at all. They pin
  # `dev` outright, which is exactly what a machine with no canonical clone
  # resolved to before -- the drift guard fails OPEN there, having no
  # canonical commit to compare against -- but now says so. OmniNode
  # workspace machines, where the guard does bite, use the commit-pinned
  # variant documented in `skills/delegate/SKILL.md`; that one reads
  # OMNIBASE_PATH fail-fast (`:?`), so a missing workspace root refuses
  # loudly instead of resolving to the wrong directory.
  #
  # Proven end-to-end 2026-08-25 and re-proven 2026-08-31: a scratch venv
  # built from exactly this recipe passes `check_omnimarket_drift()` with
  # zero drift and resolves `node_delegate_skill_orchestrator` from the
  # installed distribution
  # (`test_install_works.py::test_declared_pins_install_and_delegate_runs`).
  # Do NOT document `uv run onex delegate` — `uv run` resolves the venv of
  # whatever project the CURRENT directory belongs to, so it only works inside
  # a repo that happens to co-install omnibase-infra (see cwd_independent below).
  install_hint: "uv tool install --with 'omnibase-infra>=0.38.4' --with 'omnimarket @ git+https://github.com/OmniNode-ai/omnimarket.git@dev' 'omnibase-core>=0.46.8'"
  install_hint_pipx: "pipx install 'omnibase-core>=0.46.8' && pipx inject omnibase-core 'omnibase-infra>=0.38.4' 'omnimarket @ git+https://github.com/OmniNode-ai/omnimarket.git@dev'"
  cwd_independent: true
  # GAP CLOSED 2026-08-16 (OMN-16041); RE-QUALIFIED 2026-08-18 (OMN-16191);
  # RE-QUALIFIED AGAIN 2026-08-25 (OMN-16528) — the git+URL omnimarket
  # reference above is not a PyPI package version, so "installable_from_pypi"
  # is no longer literally 100% accurate; it still means what the paired
  # xfail marker in test_install_works.py cares about: the declared onex_cli
  # recipe is provably installable AND produces a working `onex delegate`,
  # proven fresh by `test_declared_pins_install_and_delegate_runs` with no
  # xfail. `omnibase-core`/`omnibase-infra` remain plain PyPI version pins;
  # only `node_package` resolves via git (see install_hint above for why).
  installable_from_pypi: true

# delegate's only backing node. Topic/schema entries intentionally omitted:
# node_delegate_skill_orchestrator dispatches via `onex delegate` (CLI ->
# omnibase_infra cli_delegate.py -> node dispatch), not through the runtime
# dispatch-engine topic matrix this file otherwise tracks for plugins/onex.
nodes: []
