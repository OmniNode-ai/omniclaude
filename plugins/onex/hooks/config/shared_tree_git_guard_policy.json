{
    "$comment": [
        "OMN-18798. The vocabulary of a destructive git command this guard refuses",
        "when the command's effective git root is the REGISTRY CLONE at $OMNI_HOME",
        "itself, and the sanctioned alternatives it points a caller at.",
        "",
        "Read by plugins/onex/hooks/lib/shared_tree_git_guard.py, which is invoked",
        "by the pre_tool_use_shared_tree_git_guard.sh PreToolUse hook. The",
        "vocabulary lives here, not in the checker, matching the OMN-17334",
        "git-stash policy this file is patterned on: adding a shape is a config",
        "edit visible in one place rather than a rule a reader has to infer from a",
        "Python literal.",
        "",
        "Why this exists",
        "----------------",
        "The registry clone at $OMNI_HOME is the ONE repository in the fleet where",
        "many concurrent agent",
        "lanes work in the SAME working tree (docs/tracking/, docs/workflows/,",
        ".claude/workflows/, scripts/). Code repos use per-ticket worktrees and do",
        "not have this problem. Two measured failure classes:",
        "",
        "  1. DESTRUCTION. `git reflog` recorded 20 `reset: moving to origin/main`",
        "     entries in the shared clone on 2026-09-18 alone. Those resets twice",
        "     re-orphaned a staged 1.9 MB ledger-roll archive holding 1,258",
        "     append-only coordination rows, leaving them untracked on one disk",
        "     with no copy in any commit, on any branch, on any remote, for 2h22m;",
        "     a `git clean -fd` in that window would have destroyed them and left",
        "     origin/main showing a ledger that had never lost them. The same",
        "     verbs twice dropped a peer lane's unpushed commit from local main",
        "     and twice reverted a peer lane's uncommitted edit (OMN-18798,",
        "     the registry repo pull requests 407 and 408).",
        "",
        "  2. STRANDING, which destroys nothing and is quieter. A feature branch",
        "     checked out in the shared clone makes commit_lock.py refuse EVERY",
        "     other lane's ledger commit with exit 78, STRANDED CLONE, for as long",
        "     as it stays checked out. Measured by lane",
        "     dev-lane-broker-recreate-on-warm-deploy-diag-0125: three rows",
        "     appended at 01:24Z reached a committed copy only at 11:17Z, and the",
        "     refusals at 02:37:19Z and 02:37:41Z named the branch. The only",
        "     signal is an exit code on somebody else's terminal. This is why",
        "     `git checkout -b` and `git switch -c` are refused here and not only",
        "     the destructive verbs.",
        "",
        "Doctrine: the workspace CLAUDE.md Operating Rule 19, whose closing",
        "paragraph (landed as registry pull request 410) states the prohibition",
        "this guard mechanises.",
        "Rule 19 was doctrine-only until this ticket.",
        "",
        "refused_subcommands -- the git subcommands this guard evaluates when the",
        "  effective git root IS the registry clone. `reset`, `switch`, `clean` and",
        "  `rebase` are refused in every form. `checkout` and `branch` carry their",
        "  own refinement below, because each has a sanctioned shape.",
        "",
        "checkout: refused EXCEPT the path-scoped restore recipe Operating Rule 17",
        "  sanctions -- `git checkout -- <path>` and `git checkout <ref> -- <path>`",
        "  -- which touches only the named paths and no shared state. Three",
        "  exceptions to the exception: a `-b`/`-B` branch creation is refused even",
        "  with a `--` (it is the stranding shape); a path operand naming the whole",
        "  tree (blanket_path_operands) is refused because it is a blanket checkout",
        "  wearing path-scoped syntax, which is the verb Rule 19 names; and a",
        "  checkout with operands but NO `--` separator is refused because git",
        "  itself cannot tell a ref from a path there, so neither can this guard --",
        "  fail closed and use the explicit form. A bare `git checkout` with no",
        "  operands changes nothing and is not refused.",
        "",
        "merge: refused UNLESS the invocation is one of exactly TWO shapes.",
        "",
        "  (1) It carries one of merge_allowed_flags. `--ff-only` either advances",
        "  the pointer or refuses, so it never runs a content merge and cannot",
        "  resolve a peer lane's rows away. `--abort`, `--quit` and `--continue`",
        "  manage a merge that is ALREADY in progress -- refusing them would",
        "  strand the tree mid-conflict, which is worse than the state the guard",
        "  is protecting.",
        "",
        "  (2) THE LEDGER PUBLISH LOOP: exactly one operand, drawn from",
        "  merge_allowed_targets, with no flags outside",
        "  merge_target_allowed_flags, AND the clone checked out on a branch in",
        "  merge_allowed_on_branches. In practice: `git merge origin/main` or",
        "  `git merge --no-edit origin/main`, on `main`.",
        "",
        "  This second arm is a NON-fast-forward merge, and the first revision of",
        "  this guard refused it. That was wrong, and refusing it would have",
        "  broken the only working publish path the moment the hook went live in",
        "  a new session. Operator ruling, 2026-09-19: the `--ff-only` step lost",
        "  the race against concurrent appends EIGHT cycles running, so the ruled",
        "  loop is commit_lock rows, then `git merge --no-edit origin/main`, then",
        "  a push to a FRESH branch, a pull request, and a merge. Lanes",
        "  ledger-publish-1655 and -1750 ran it cleanly (registry pull requests",
        "  439-443).",
        "",
        "  THE BRANCH PRECONDITION IS THE WHOLE CONTROL, not a detail. The",
        "  2026-09-19 14:52:55Z loss was this same verb against this same target",
        "  -- but run on a FEATURE BRANCH checked out in the shared clone, and",
        "  the round trip back to `main` at 14:55:04Z did not carry rows appended",
        "  between 14:48Z and 14:53Z. On `main` the merge either fast-forwards or",
        "  makes one merge commit on the branch every lane is already committing",
        "  to; off `main` it resolves the shared ledger against a divergent side",
        "  history. Same command, different blast radius. An unreadable HEAD",
        "  fails closed, because the precondition cannot then be checked.",
        "",
        "  The allowance is deliberately narrow: one target, one optional flag,",
        "  one branch. A different target, a second operand, or any strategy flag",
        "  (`--no-ff`, `--squash`, `-X theirs`) is refused.",
        "",
        "branch: refused for a DELETE or RENAME of the currently checked-out",
        "  branch (branch_destructive_flags, target read from .git/HEAD rather",
        "  than by running git), and refused for CREATION -- any positional",
        "  operand with no destructive flag and no flag from branch_read_flags.",
        "",
        "  Creation was allowed by the first revision of this guard, on the",
        "  reasoning that `git branch <name>` moves no working tree. That is",
        "  true and it is not the hazard: a branch created in the shared clone",
        "  exists in order to be checked out, and the moment it is,",
        "  commit_lock.py refuses EVERY other lane's ledger commit with exit 78,",
        "  STRANDED CLONE. Refusing only the checkout leaves the first half of a",
        "  two-step hazard admitted and points nobody at the remedy. The refusal",
        "  names the worktree form, which creates the branch and the place to",
        "  use it in one command.",
        "",
        "  branch_read_flags keeps a LISTING from being mistaken for a creation:",
        "  `git branch --list 'lane/*'`, `--contains HEAD` and `--merged",
        "  origin/main` all carry an operand that is a filter, not a new branch.",
        "  Matched on the part before any `=` so `--format=%(refname)` counts.",
        "  `git branch -d <some-other-branch>` is still allowed: it neither moves",
        "  this tree nor strands it.",
        "",
        "allowed_always -- recorded for the reader, NOT consulted by the checker.",
        "  These shapes are allowed because they are not in refused_subcommands at",
        "  all, which is the stronger statement: there is no allowlist to fall out",
        "  of date. `git merge --ff-only`, the publish-loop `git merge",
        "  --no-edit origin/main` on `main`, `git fetch`, `git pull --ff-only`,",
        "  an ordinary `git push`, and every read (status, log, diff, show,",
        "  rev-parse, reflog, worktree list) never match a refused shape.",
        "",
        "registry_root_envs / registry_root_markers -- how the registry clone is",
        "  identified. The env vars are tried first, in order. The markers are the",
        "  fallback so the guard does not go DARK when the environment is thin: a",
        "  hook process with no OMNI_HOME would otherwise admit every refused verb",
        "  in the exact tree this guard exists to protect. A directory matches only",
        "  when it is a git root AND carries every marker.",
        "",
        "Scope, stated so it is not over-read: this guard fires ONLY when the",
        "effective git root is the registry clone itself. A worktree under",
        "omni_worktrees/ has its own .git file and its own root, so it is never",
        "matched; neither is a code-repo canonical clone under $OMNI_HOME, whose",
        "root is $OMNI_HOME/<repo> and not $OMNI_HOME. Those surfaces already have",
        "the OMN-7018/OMN-14330 worktree guard.",
        "",
        "There is deliberately NO escape entry -- no consent citation, no wildcard",
        "shape, no exempt marker. Every refused verb has a sanctioned alternative",
        "that reaches the same outcome without touching state a peer lane owns, so",
        "there is nothing a legitimate use needs that this guard blocks.",
        "",
        "Deliberately NOT refused, and named rather than left silent: `git restore`",
        "and `git restore --staged`, which can be as blanket as a checkout. They",
        "are outside the verb set this ticket's brief enumerated, and adding a verb",
        "nobody measured is how a guard starts refusing work it was never shown to",
        "protect. Add it when a collision is measured, with the measurement."
    ],
    "ticket": "OMN-18798",
    "rule": "Operating Rule 19",
    "refused_subcommands": [
        "reset",
        "checkout",
        "switch",
        "clean",
        "rebase",
        "branch",
        "merge",
        "push"
    ],
    "unconditional_subcommands": [
        "reset",
        "switch",
        "clean",
        "rebase"
    ],
    "branch_destructive_flags": [
        "-d",
        "-D",
        "--delete",
        "-m",
        "-M",
        "--move"
    ],
    "branch_creation_flags": [
        "-b",
        "-B",
        "-c",
        "-C"
    ],
    "branch_read_flags": [
        "-l",
        "--list",
        "-a",
        "--all",
        "-r",
        "--remotes",
        "-v",
        "-vv",
        "--verbose",
        "--show-current",
        "--contains",
        "--no-contains",
        "--merged",
        "--no-merged",
        "--points-at",
        "--format",
        "--sort",
        "--column",
        "--color",
        "-i",
        "--ignore-case",
        "--omit-empty"
    ],
    "merge_allowed_flags": [
        "--ff-only",
        "--abort",
        "--quit",
        "--continue"
    ],
    "merge_allowed_targets": [
        "origin/main"
    ],
    "merge_target_allowed_flags": [
        "--no-edit"
    ],
    "merge_allowed_on_branches": [
        "main"
    ],
    "blanket_path_operands": [
        ".",
        "./",
        "*",
        ":/",
        ":(top)"
    ],
    "push_force_flags": [
        "-f",
        "--force",
        "--force-with-lease",
        "--force-if-includes"
    ],
    "protected_push_refs": [
        "dev",
        "main"
    ],
    "directory_changing_programs": [
        "cd",
        "pushd"
    ],
    "protected_path_operands": [
        "docs/tracking/ROLLING_WORK_LEDGER.md",
        "docs/tracking/archive"
    ],
    "safe_alternatives": "do the work in a worktree under $OMNI_HOME/omni_worktrees/<ticket>/<repo> instead of the shared clone, created with git worktree add -b so the branch and the place to use it arrive together; sync with git -C $OMNI_HOME merge --ff-only origin/main, or run the ruled ledger publish loop on main -- commit_lock rows, git merge --no-edit origin/main, push a FRESH branch, open a pull request and land it; append ledger rows through scripts/ledger_lock.py and commit them through scripts/commit_lock.py; or restore individual files with a path-scoped git checkout <ref> -- <path> naming each path, which is refused only on the append-only ledger itself",
    "registry_root_envs": [
        "OMNI_HOME",
        "ONEX_REGISTRY_ROOT"
    ],
    "registry_root_markers": [
        "docs/tracking/ROLLING_WORK_LEDGER.md",
        ".kb-doc-gate.yaml"
    ]
}
