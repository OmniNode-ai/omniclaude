{
  "$schema": "https://claude.ai/schemas/hooks.json",
  "description": "OmniClaude Core Hooks - ALL HOOKS DISABLED for the measurement baseline (OMN-13244). Every registration was removed so Claude Code invokes no onex hooks; the scripts remain on disk and re-registration is a pure config change. Restore from git history / revert OMN-13244 to re-enable.",
  "version": "1.3.0",
  "hooks": {}
}
