{
  "$schema": "https://claude.ai/schemas/hooks.json",
  "description": "OmniClaude Core Hooks - OMN-13244 measurement baseline with the OMN-13856 and OMN-14330 carve-outs. Every context-injection/measurement hook stays DISABLED; the only re-registered hooks are the Done-flip durable-evidence guard (OMN-13856, operator-approved, no fake Done) and the OMN-7018 worktree canonical-root guard (OMN-14330, blocks `git worktree add` outside $OMNI_HOME/omni_worktrees). All other scripts remain on disk and re-registration stays a pure config change.",
  "version": "1.4.0",
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "^mcp__linear-server__(save_issue|update_issue)$",
        "hooks": [
          {
            "type": "command",
            "command": "${CLAUDE_PLUGIN_ROOT}/hooks/scripts/pre_tool_use_done_flip_guard.sh"
          }
        ]
      },
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "${CLAUDE_PLUGIN_ROOT}/hooks/scripts/pre_tool_use_worktree_guard.sh"
          }
        ]
      }
    ]
  }
}
