{
  "$schema": "https://claude.ai/schemas/hooks.json",
  "description": "OmniClaude Core Hooks - OMN-13244 measurement baseline with the OMN-13856 Option A carve-out. Every context-injection/measurement hook stays DISABLED; the ONLY re-registered hook is the Done-flip durable-evidence guard (operator-approved, no fake Done). All other scripts remain on disk and re-registration stays a pure config change.",
  "version": "1.3.0",
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "^mcp__linear-server__(save_issue|update_issue)$",
        "hooks": [
          {
            "type": "command",
            "command": "${CLAUDE_PLUGIN_ROOT}/hooks/scripts/pre_tool_use_done_flip_guard.sh"
          }
        ]
      }
    ]
  }
}
