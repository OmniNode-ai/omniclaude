---
schema_version: "1.0.0"
ticket_id: "OMN-13090"
title: "Document updatedToolOutput shape probe evidence"
summary: >
  Adds the updatedToolOutput shape probe report for the skill-output-suppression work. The PR is
  evidence-only research plus pre-existing gate cleanup; no production Layer C suppression code ships here.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-probe-doc"
    description: >-
      The updatedToolOutput shape probe document exists. OMN-16609 migrated it out of
      docs/research/ into the knowledge base at
      reference/claude-code-posttooluse-output-shape.md; this repo carries the citation.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q 'knowledge-base:reference/claude-code-posttooluse-output-shape.md' plugins/onex/hooks/lib/post_tool_use_secret_redact_guard.py"
  - id: "dod-deploy-not-required"
    description: "No live deploy is required for this evidence-only probe and comment/header cleanup."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q 'No live deploy is required' contracts/OMN-13090.yaml"
