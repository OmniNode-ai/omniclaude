---
schema_version: "1.0.0"
ticket_id: "OMN-13571"
title: "Sanitize omniclaude documentation ticket identifiers"
summary: >-
  Documentation-only cleanup for omniclaude public-repo hygiene. The PR replaces historical ticket identifiers
  in doc prose and portable examples with generic descriptions or example ticket IDs, and renames OMN-named
  documentation files to descriptive names. No runtime code, hook behavior, topic schema, or deployment surface
  changes.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "ci"
    description: "PR checks pass after documentation identifier sanitization."
    command: "gh pr checks 1800 --repo OmniNode-ai/omniclaude"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-doc-identifier-sanitization"
    description: "The PR is documentation-only and removes historical ticket identifiers from public-facing docs."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "test -f contracts/OMN-13571.yaml"
  - id: "dod-no-deploy-required"
    description: "No live deploy is required for documentation-only identifier sanitization."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q 'No runtime code' contracts/OMN-13571.yaml"
