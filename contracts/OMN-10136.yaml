---
schema_version: "1.0.0"
ticket_id: OMN-10136
summary: "Extend ModelDelegationCommand with Cross-CLI Bridge routing fields while preserving auto-recipient defaults"
is_seam_ticket: true
interface_change: true
interfaces_touched:
  - "src/omniclaude/nodes/node_delegation_orchestrator/models/model_delegation_command.py"
evidence_requirements:
  - kind: tests
    description: "Delegation command model tests cover recipient, wait_for_result, working_directory, sandbox mode, validation errors, and backward compatibility"
    command: "uv run pytest tests/unit/node_delegation_orchestrator/test_model_delegation_command.py -q"
  - kind: ci
    description: "omniclaude PR #1436 CI green across required gates"
    command: "gh pr checks 1436 --repo OmniNode-ai/omniclaude --watch"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: dod-001
    description: "Delegation command model behavior is covered by focused unit tests"
    source: manual
    checks:
      - check_type: command
        check_value: "uv run pytest tests/unit/node_delegation_orchestrator/test_model_delegation_command.py -q"
  - id: dod-002
    description: "Runtime-adjacent delegation command schema change has a deploy verification surface before merge"
    source: manual
    checks:
      - check_type: command
        check_value: "uv run pytest tests/unit/node_delegation_orchestrator/test_model_delegation_command.py -q  # deploy-verify ModelDelegationCommand schema extension"
