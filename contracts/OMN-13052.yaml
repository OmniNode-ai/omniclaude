---
schema_version: "1.0.0"
ticket_id: "OMN-13052"
title: "D-6: UI verification line in dispatch template"
summary: >
  Add an eighth auto-injected Operating Rule to the dispatch_worker skill
  (prompt.md + SKILL.md) so every compiled worker prompt requires Playwright
  proof for UI DoD items — live URL, screenshot, and network log of the actual
  emitted request — and explicitly rejects a curl of the canonical endpoint as
  evidence for a UI claim. Bridges the gap until the A-2 Receipt-Gate
  evidence-class check (OMN-13024) is live. Skill-markdown + tests only; no
  runtime dependency.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-ui-verification-rule"
    description: >
      prompt.md and SKILL.md both carry the UI-verification operating rule with
      the Playwright requirement (operator's running surface + screenshot +
      network log) and the explicit curl-rejection for UI claims. Enforced by
      the dispatch_worker injection tests.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "env -u PYTHONPATH uv run pytest tests/skills/test_dispatch_worker_operating_rules_injection.py -q"
  - id: "dod-occ-authority"
    description: >
      Central OCC contract and DoD receipts for OMN-13052 are linked from the
      code PR body via Evidence-Source + Evidence-Ticket.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q 'Evidence-Ticket: OMN-13052' /tmp/pr_body.txt"
