---
schema_version: "1.0.0"
ticket_id: "OMN-13044"
title: "C-4: Worktree durability sweep — NO-TICKET + dirty-plan-file flags, rescue refs, off-volume backups"
summary: >-
  Worktree durability sweep for the worktree skill and audit logic: flag NO-TICKET
  worktrees (directory name carries no OMN-NNNN id), flag dirty worktrees touching
  files referenced by docs/plans/ or docs/handoffs/ documents, auto-create a
  rescue/<ticket>/<timestamp> ref over git stash create BEFORE a handoff-block, and
  require an off-volume backup copy (ticket attachment or docs-branch commit) for
  demo-critical .onex_state backups to count toward DoD. Retains the reaper boundary:
  the OMN-13228 Mac worktree reaper delegates dirty-worktree salvage to OMN-13044 and
  preserves the existing prune-worktrees.sh safety behavior.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-salvage-out-of-scope-reference"
    description: >-
      The reaper implementation documents salvage as out of scope and delegates
      cleanup safety to prune-worktrees.sh.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q "OMN-13044 owns" scripts/worktree_reaper.py
  - id: "dod-pr-contract"
    description: "Repo-local PR contract exists for the worktree durability sweep update."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "test -f contracts/OMN-13044.yaml && grep -q 'OMN-13044' contracts/OMN-13044.yaml"
  - id: "dod-skill-durability-section"
    description: "SKILL.md worktree audit lists NO-TICKET, dirty-plan-file, rescue-ref, and off-volume backup steps."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q 'Durability sweep (OMN-13044)' plugins/onex/skills/worktree/SKILL.md && grep -q 'NO-TICKET detection' plugins/onex/skills/worktree/SKILL.md && grep -q 'Rescue-ref auto-creation' plugins/onex/skills/worktree/SKILL.md && grep -q 'Off-volume backup requirement' plugins/onex/skills/worktree/SKILL.md"
  - id: "dod-durability-helpers"
    description: "Pure durability helpers + flags model exist and are unit-tested."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q 'def build_rescue_ref' src/omniclaude/hooks/lib/worktree_health.py && grep -q 'class ModelWorktreeDurabilityFlags' src/omniclaude/hooks/lib/worktree_health.py && test -f tests/unit/hooks/lib/test_worktree_durability.py"
