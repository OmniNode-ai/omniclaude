---
schema_version: "1.0.0"
ticket_id: OMN-16301
title: "Delete dead doc_freshness_sweep skill"
summary: "Remove the doc_freshness_sweep skill and its dangling backing-node references after the upstream omnimarket node was deleted (OMN-16191) and the omnibase_infra skill_mapping.yaml entry was already removed on dev."
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: tests
    description: "Skill backing-node liveness self-scan and the touched test suites pass."
    command: "OMNI_HOME=$OMNI_HOME uv run python plugins/onex/skills/_lib/validate_skill_backing_node.py ."
  - kind: manual
    description: "Central OCC evidence binds OMN-16301 proof."
    command: "Evidence-Source: OCC#6817"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: dod-skill-backing-node-liveness
    description: "validate_skill_backing_node.py self-scan against the live sibling omnimarket clone reports 30/30 backing-node declarations live, with doc_freshness_sweep no longer among them."
    source: manual
    checks:
      - check_type: command
        check_value: "OMNI_HOME=$OMNI_HOME uv run python plugins/onex/skills/_lib/validate_skill_backing_node.py ."
  - id: dod-dispatch-skill-receipt-contract
    description: "tests/unit/skills/test_dispatch_skill_receipt_contract.py passes with doc_freshness_sweep removed from ONEX_SKILL_SHIMS."
    source: manual
    checks:
      - check_type: command
        check_value: "uv run pytest tests/unit/skills/test_dispatch_skill_receipt_contract.py -q"
  - id: dod-plugin-compat-schema
    description: "plugin-compat.yaml schema tests pass with node_doc_freshness_sweep removed and the R-class node count fixture updated 47 -> 46."
    source: manual
    checks:
      - check_type: command
        check_value: "uv run pytest plugins/onex/tests/test_plugin_compat_yaml.py -q"
  - id: dod-distribution-classification
    description: "Distribution classification validator reports all discovered components classified after removing the doc_freshness_sweep manifest entry."
    source: manual
    checks:
      - check_type: command
        check_value: "uv run python scripts/validation/validate_distribution_classification.py"
  - id: dod-occ-evidence
    description: "Central OCC evidence for OMN-16301 is merged and referenced by this PR body."
    source: manual
    checks:
      - check_type: command
        check_value: "grep -q 'Evidence-Source: OCC#6817' /tmp/pr_body.txt"
